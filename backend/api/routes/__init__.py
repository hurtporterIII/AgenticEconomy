"""Route modules for AgenticEconomy API (split from api.endpoints).

Router aggregation into sub-routers is done in later migration steps.
The public app still imports ``router`` from ``api.endpoints``.
"""
